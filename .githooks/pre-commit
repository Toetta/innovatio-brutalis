#!/bin/sh
# Keep the mirrored FU-Bookkeeping copy in sync with the source repo when available.
# Requires: node

set -e

node tools/sync-fu-bookkeeping-mirror.mjs

if [ -f "external/FU-Bookkeeping/FU-Bookkeeping.html" ]; then
	git add external/FU-Bookkeeping/FU-Bookkeeping.html
fi
